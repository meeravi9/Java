package SRP;

public class ReportGenerator {
    public void generateReport(BankAccount account) {
        // Logic to generate a report based on the account details
        System.out.println("Generating report for account number: " + account.getAccountNumber());
        // ...
    }
}
